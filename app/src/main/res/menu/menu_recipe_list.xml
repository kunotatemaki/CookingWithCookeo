<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools" tools:context=".RecipeListActivity">
    <item
            android:id="@+id/action_search"
            android:title="@android:string/search_go"
            android:icon="@drawable/ic_search_white_24dp"
            app:showAsAction="ifRoom|collapseActionView"
            app:actionViewClass="androidx.appcompat.widget.SearchView"
            android:visible="true"/>

    <item
            android:id="@+id/menu_sign_in"
            android:title="@string/sign_in"
            android:orderInCategory="101"
            app:showAsAction="never" />
    <item
            android:id="@+id/menu_sign_out"
            android:title="@string/sign_out"
            android:orderInCategory="102"
            app:showAsAction="never" />
    <item
            android:id="@+id/menu_thanks"
            android:title="@string/thanks"
            android:orderInCategory="103"
            app:showAsAction="never" />

</menu>